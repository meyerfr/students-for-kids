
<div class="show-wrapper">
  <%= render '/shared/random_hand_background' %>
  <div class="show-container">
    <% if current_user == @sitter || current_user.class = Admin || current_user.sitters.slect{|s| s.id == @sitter.id}.present? %>
      <%= link_to '<i class="far fa-edit"></i>'.html_safe, edit_sitter_path(@sitter), class: 'edit-button' %>
    <% end %>
    <div class="form-inputs show">
      <div class="profile-photo-container">
        <% if @sitter.photo.attached? %>
          <%= cl_image_tag(@sitter.photo.key, width: 100, height: 100, gravity: 'face:center', crop: :fill, effect: :sharpen) %>
        <% else %>
          <i class="fas fa-user"></i>
        <% end %>
      </div>
      <div class="form-group">
        <%= label_tag 'Vorname:' %>
        <%= @contact_info.first_name %>
      </div>
      <div class="form-group">
        <%= label_tag 'Nachname:' %>
        <%= @contact_info.last_name %>
      </div>
      <div class="form-group">
        <%= label_tag 'Alter:' %>
        <%= @sitter.age %>
      </div>
      <div class="form-group d-flex flex-column">
        <%= label_tag 'Bio:' %>
        <%= @contact_info.bio %>
      </div>
      <% if current_user == @sitter || current_user.sitters.slect{|s| s.id == @sitter.id}.present? %>
        <div class="form-group">
          <%= label_tag 'phone', class: 'info-label' do %>
            <span>Telefonnummer:</span>
            <div class="info-on-hover">
              <i class="fas fa-info"></i>
              <div class="info">
                <span>Wird nur dir und deinen Kunden gezeigt.</span>
              </div>
            </div>
          <% end %>
          <%= @contact_info.phone %>
        </div>
      <% end %>
      <% if current_user == @sitter %>
        <div class="form-group">
          <%= label_tag 'street', class: 'info-label' do %>
            <span>Straße:</span>
            <div class="info-on-hover">
              <i class="fas fa-info"></i>
              <div class="info">
                <span>Wird nur dir gezeigt.</span>
              </div>
            </div>
          <% end %>
          <%= @contact_info.street %>
        </div>
        <div class="form-group">
          <%= label_tag 'post_code', class: 'info-label' do %>
            <span>Postleitzahl:</span>
            <div class="info-on-hover">
              <i class="fas fa-info"></i>
              <div class="info">
                <span>Wird nur dir gezeigt.</span>
              </div>
            </div>
          <% end %>
          <%= @contact_info.post_code %>
        </div>
        <div class="form-group">
          <%= label_tag 'city', class: 'info-label' do %>
            <span>Stadt:</span>
            <div class="info-on-hover">
              <i class="fas fa-info"></i>
              <div class="info">
                <span>Wird nur dir gezeigt.</span>
              </div>
            </div>
          <% end %>
          <%= @contact_info.city %>
        </div>
      <% end %>
    </div>
  </div>
</div>
<%#= link_to 'Back', sitters_path %>
